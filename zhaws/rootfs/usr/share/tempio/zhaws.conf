{
    "logConfig": {
        "enabled": true,
        "level": "{{ .log_level }}",
        "forceConsole": true
    },
    "storage": {
        "cacheDir": "/data/cache",
        "throttle": "slow"
    },
    "enableSoftReset": {{ .soft_reset }}
}
