{
    "host": "{{ .host }}",
    "port": "{{ .port }}",
    "network_auto_start": "{{ .network_auto_start }}",
    "zigpy_configuration": {
        "database_path": "/data/db/zigbee.db",
        "enable_quirks": "{{ .enable_quirks }}"
    },
    "radio_configuration": {
        "type": "{{ .radio_type }}",
        "path": "{{ .radio_path }}",
        "baudrate": "{{ .radio_baudrate }}",
        "flow_control": "{{ .radio_flow_control }}"
    }
}
